# MCP Server 36 Tools Implementation Plan

> **Status: COMPLETE** — All 36 tools implemented and tested (69/69 tests passing).

**Goal:** Expand portfolio_server.py from 13 to 36 MCP tools covering the full FastAPI backend.

**Architecture:** Single file (`openclaw/mcp/portfolio_server.py`), stdlib-only Python. Each tool = TOOLS entry + `_call_tool` match case. Tests mock `_http_request` to verify dispatch logic without needing a running backend. A `_job_path` helper builds URLs for all 12 job-based tools.

**Tech Stack:** Python stdlib (json, urllib), pytest for tests

---

## Chunk 1: Test Infrastructure + Tool Definitions

### Task 1: Write test infrastructure and tests for all 36 tools — DONE

**Files:**
- Created: `tests/test_mcp_server.py` (69 tests)

### Task 2: Add 23 new tool definitions to TOOLS list — DONE

**Files:**
- Modified: `openclaw/mcp/portfolio_server.py` (TOOLS array — 36 entries)

### Task 3: Add 23 new match cases to _call_tool — DONE

**Files:**
- Modified: `openclaw/mcp/portfolio_server.py` (_call_tool + _job_path helper)

### Task 4: Run tests, fix any failures — DONE

69/69 tests passing.

### Task 5: Update portfolio-mcp skill with all 36 tools — DONE

**Files:**
- Modified: `openclaw/skills/portfolio-mcp/SKILL.md`

### Task 6: Update MCP plan doc — DONE

**Files:**
- Modified: `docs/MCP_plan.md`
